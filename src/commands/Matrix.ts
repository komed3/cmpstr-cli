'use strict';

import { type Command } from 'commander';
import { CmpStrAsync } from 'cmpstr';
import { readList } from '../utils/ReadList.js';

export async function matrix ( input: string, opt: Record<string, any> = {}, cmd: Command ) : Promise<void> {

    //

}